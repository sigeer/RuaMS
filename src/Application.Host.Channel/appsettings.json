{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // 服务发现读取
  "Services": {
    "ruams-master": {
      "grpc": "http://127.0.0.1:7878"
    }
  },
  "AllowedHosts": "*",
  // 生成long型id种子，不可重复
  "LongIdSeed": 2,
  "ChannelServerConfig": {
    // 不可重复
    "ServerName": "ChannelNode1",
    // Channel的Host会返回给客户端，供客户端连接
    "ServerHost": "127.0.0.1",
    // 供MasterServer连接
    "ChannelConfig": [
      {
        "Port": 7578,
        "MaxSize": 20
      },
      {
        "Port": 7579,
        "MaxSize": 20
      },
      {
        "Port": 7580,
        "MaxSize": 20
      }
    ],
    "SystemConfig": {
      "AutoClearMap": true
    }
  }
}
